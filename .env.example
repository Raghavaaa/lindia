# ==========================================
# LegalIndia.ai Environment Configuration
# ==========================================

# ----- API Keys (REQUIRED) -----
# DeepSeek API key for AI-powered legal research
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Hugging Face token (if using HF models)
HF_TOKEN=your_huggingface_token_here

# ----- Admin Configuration -----
# Secret key for admin operations
ADMIN_SECRET=your_admin_secret_here

# ----- Frontend Configuration -----
# API base URL for frontend (use /api for same-origin or full URL for separate backend)
NEXT_PUBLIC_FRONTEND_API_BASE=/api
NEXT_PUBLIC_FRONTEND_ENV=production

# Public API URL (for external integrations)
NEXT_PUBLIC_API_URL=https://your-app.railway.app

# ----- Application Settings -----
# Node environment
NODE_ENV=production

# Custom port (Railway sets this automatically)
PORT=3000

# ----- Optional: AI Configuration -----
# Base prompt for legal research refinement
PROMPT_BASE=Refine and clarify the legal research query for Indian law.

# ----- Optional: Database (for future use) -----
# DATABASE_URL=postgresql://user:password@host:port/database

# ----- Optional: Redis Cache (for future use) -----
# REDIS_URL=redis://host:port

# ----- Optional: Monitoring -----
# SENTRY_DSN=your_sentry_dsn_here
# LOG_LEVEL=info
